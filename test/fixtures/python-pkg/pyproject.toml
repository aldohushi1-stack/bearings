[project]
name = "mypkg"
version = "0.4.0"
description = "Parses widget manifests into reports."
requires-python = ">=3.10"
dependencies = ["click>=8"]

[project.scripts]
mypkg = "mypkg.cli:main"

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
testpaths = ["tests"]
